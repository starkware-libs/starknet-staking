name: Check Formatting

on:
  workflow_call:

jobs:
  check_formatting:
    runs-on: namespace-profile-starkware-libs-starknet-staking-large-ubuntu-24-04-amd64

    steps:
      - uses: actions/checkout@v4
      - uses: software-mansion/setup-scarb@v1
        with:
          scarb-version: "2.12.2"

      - name: Check formatting
        run: |
          scarb fmt -w --check
