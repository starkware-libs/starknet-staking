{
  "name": "@staking/contracts",
  "version": "1.0.1-dev.1009",
  "private": true,
  "description": "staking contracts",
  "scripts": {
    "build": "scarb build",
    "clean": "scarb clean && snforge clean-cache",
    "dev": "scarb cairo-run",
    "format": "pnpm sort-package-json && scarb fmt",
    "lint": "scarb fmt --check",
    "test": "scarb test",
    "check-flow-tests": "python3 ../../../../scripts/staking/check_all_flows_have_test.py --allow-duplicate-tests"
  }
}
